apiVersion: apps/v1
kind: Deployment
metadata:
  name: dish-command-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: dish-command
  template:
    metadata:
      labels:
        app: dish-command
    spec:
      containers:
        - name: dish-command
          image: {{ .Values.dishcommand.image }}
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"